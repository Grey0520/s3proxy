s3proxy:
  endpoint: 127.0.0.1:5080
  secureEndpoint: https://127.0.0.1:5443

cloud:
  provider: local
  endpoint: does'n need
  identity: dn
  key: dn
  appid: dn
  credential: dn
  filesystem:
    basedir: /tmp/s3store

# 更改配置文件 config.yaml 后， 需要更改 internal/config/config.go
# 注意：这是一份示例文件
